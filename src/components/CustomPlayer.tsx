"use client";

// Backward-compatible alias: existing app code should move to StreamPlayer import.
export { StreamPlayer as default } from "hls-react-player";
export type { StreamPlayerProps as CustomPlayerProps, StreamType } from "hls-react-player";
